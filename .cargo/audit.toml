[advisories]
ignore = [
    "RUSTSEC-2023-0071", # rsa: Marvin Attack — transitive via sqlx-mysql, no upstream fix
    "RUSTSEC-2024-0436", # paste: unmaintained — transitive via rav1e/image, no alternative
    "RUSTSEC-2025-0134", # rustls-pemfile: unmaintained — transitive via tonic/qdrant-client
    "RUSTSEC-2026-0002", # lru: unsound IterMut — transitive via aws-sdk-s3, no fix available
    "RUSTSEC-2026-0097", # rand 0.8/0.9: unsound with custom logger — transitive via jsonwebtoken, sqlx, tonic
    "RUSTSEC-2026-0098", # rustls-webpki: URI name constraint — transitive via aws-sdk-s3 (pinned rustls 0.21)
    "RUSTSEC-2026-0099", # rustls-webpki: wildcard name constraint — transitive via aws-sdk-s3 (pinned rustls 0.21)
    "RUSTSEC-2026-0104", # rustls-webpki: CRL parsing panic — transitive via aws-sdk-s3 (pinned rustls 0.21)
]
