[advisories]
ignore = [
    # cargo-audit evaluates Cargo.lock package metadata without feature
    # resolution, so it reports image's optional avif/ravif path even though
    # rskit does not enable image/avif. cargo-deny remains the feature-aware
    # advisory gate for enabled dependency graphs.
    "RUSTSEC-2024-0436",
]
