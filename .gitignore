# Rust build artifacts — any depth (covers workspace root, fuzz/, benches/, etc.)
target/
**/*.rs.bk
*.pdb

# Fuzz sub-workspace (excluded from main workspace; its lockfile is not reproducibility-critical)
fuzz/Cargo.lock
# Fuzzing artifacts (regenerated by cargo-fuzz; not source)
fuzz/corpus/
fuzz/artifacts/
fuzz/coverage/

# Coverage
coverage.lcov
lcov.info
*.profraw
*.profdata
tarpaulin-report.html
tarpaulin-report.json
cobertura.xml

# Benchmarks
criterion/

# IDE
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.local
.secrets

# OS
.DS_Store
Thumbs.dbgraph.png

/tmp/*
!/tmp/.keep
