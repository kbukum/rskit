[package]
name        = "rskit-bench"
description = "ML benchmarking framework: evaluators, metrics, reports, visualization"
version.workspace      = true
edition.workspace      = true
license.workspace      = true
rust-version.workspace = true
authors.workspace      = true
repository.workspace   = true
homepage.workspace     = true
documentation          = "https://docs.rs/rskit-bench"
readme                 = "README.md"
categories             = ["development-tools::testing"]
keywords               = ["benchmark", "evaluation", "metrics", "ml"]

[dependencies]
# External
serde          = { workspace = true }
serde_json     = { workspace = true }
futures        = { workspace = true }
tokio          = { workspace = true }
tokio-util     = { workspace = true }
tracing        = { workspace = true }
async-trait    = { workspace = true }
parking_lot    = { workspace = true }

# Internal
rskit-errors   = { version = "0.1.0-alpha.1", path = "../rskit-errors" }
rskit-fs       = { version = "0.1.0-alpha.1", path = "../rskit-fs" }
rskit-provider = { version = "0.1.0-alpha.1", path = "../rskit-provider" }
rskit-util     = { version = "0.1.0-alpha.1", path = "../rskit-util" }
rskit-worker   = { version = "0.1.0-alpha.1", path = "../rskit-worker" }
rskit-cli      = { version = "0.1.0-alpha.1", path = "../rskit-cli" }
rskit-observability = { version = "0.1.0-alpha.1", path = "../rskit-observability" }

[dev-dependencies]
tokio     = { workspace = true, features = ["test-util", "full"] }
tempfile  = { workspace = true }
insta     = { workspace = true }
criterion = { workspace = true }
rskit     = { version = "0.1.0-alpha.1", path = "../rskit" }

[[bench]]
name    = "metrics"
harness = false

[[bench]]
name    = "core"
harness = false

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]
