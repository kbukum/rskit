/// Integration tests for rskit-logging.
/// Most logging tests are unit tests inside src/ because they depend on
/// tracing subscriber internals. This file exists as a placeholder and
/// will grow as the public API stabilises.

#[test]
fn placeholder() {}
