[package]
name        = "rskit"
description = "Production Rust toolkit — modular facade for rskit-* crates"
version.workspace     = true
edition.workspace     = true
license.workspace     = true
rust-version.workspace = true
authors.workspace      = true
repository.workspace   = true
homepage.workspace     = true
documentation = "https://docs.rs/rskit"
readme = "README.md"
categories = ["development-tools", "network-programming", "asynchronous"]
keywords   = ["toolkit", "async", "resilience", "grpc"]

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[dependencies]
# Always-on core crates
rskit-errors     = { path = "../rskit-errors" }
rskit-config     = { path = "../rskit-config" }
rskit-logging    = { path = "../rskit-logging" }
rskit-bootstrap  = { path = "../rskit-bootstrap" }
rskit-provider   = { path = "../rskit-provider" }
rskit-pipeline   = { path = "../rskit-pipeline" }
rskit-resilience = { path = "../rskit-resilience" }
rskit-worker     = { path = "../rskit-worker" }
rskit-validation = { path = "../rskit-validation" }

# Feature-gated crates
rskit-server        = { path = "../rskit-server",        optional = true }
rskit-encryption    = { path = "../rskit-encryption",    optional = true }
rskit-http          = { path = "../rskit-http",          optional = true }
rskit-httpclient    = { path = "../rskit-httpclient",    optional = true }
rskit-auth          = { path = "../rskit-auth",          optional = true }
rskit-di            = { path = "../rskit-di",            optional = true }
rskit-database      = { path = "../rskit-database",      optional = true }
rskit-cache         = { path = "../rskit-cache",         optional = true }
rskit-messaging     = { path = "../rskit-messaging",     optional = true }
rskit-observability = { path = "../rskit-observability",  optional = true }
rskit-authz         = { path = "../rskit-authz",         optional = true }
rskit-discovery     = { path = "../rskit-discovery",     optional = true }
rskit-testutil      = { path = "../rskit-testutil",      optional = true }
rskit-sse           = { path = "../rskit-sse",           optional = true }
rskit-dag           = { path = "../rskit-dag",           optional = true }
rskit-llm           = { path = "../rskit-llm",           optional = true }
rskit-file          = { path = "../rskit-file",          optional = true }
rskit-file-s3       = { path = "../rskit-file-s3",       optional = true }
rskit-media         = { path = "../rskit-media",         optional = true }
rskit-media-ffmpeg  = { path = "../rskit-media-ffmpeg",  optional = true }
rskit-media-image   = { path = "../rskit-media-image",   optional = true }
rskit-cli           = { path = "../rskit-cli",           optional = true }
rskit-dataset       = { path = "../rskit-dataset",       optional = true }
rskit-bench         = { path = "../rskit-bench",         optional = true }

[dev-dependencies]
tokio       = { workspace = true, features = ["full"] }
tokio-util  = { workspace = true }
async-trait = { workspace = true }

[features]
default       = []
server        = ["dep:rskit-server"]
encryption    = ["dep:rskit-encryption"]
http          = ["dep:rskit-http"]
httpclient    = ["dep:rskit-httpclient"]
auth          = ["dep:rskit-auth"]
di            = ["dep:rskit-di"]
database      = ["dep:rskit-database"]
cache         = ["dep:rskit-cache"]
messaging     = ["dep:rskit-messaging"]
observability = ["dep:rskit-observability"]
authz         = ["dep:rskit-authz"]
discovery     = ["dep:rskit-discovery"]
testutil      = ["dep:rskit-testutil"]
sse           = ["dep:rskit-sse"]
dag           = ["dep:rskit-dag"]
llm           = ["dep:rskit-llm"]
file          = ["dep:rskit-file"]
media         = ["dep:rskit-media", "file"]
media-ffmpeg  = ["dep:rskit-media-ffmpeg", "media"]
media-image   = ["dep:rskit-media-image", "media"]
media-full    = ["media-ffmpeg", "media-image"]
file-s3       = ["file", "dep:rskit-file-s3"]
file-gcs      = ["file", "rskit-file/gcs"]
cli           = ["dep:rskit-cli"]
dataset       = ["dep:rskit-dataset", "cli"]
bench         = ["dep:rskit-bench"]
full          = [
    "server", "encryption", "http", "httpclient", "auth", "di", "database", "cache", "messaging",
    "observability", "authz", "discovery", "testutil", "sse", "dag", "llm",
    "media-full", "file", "cli", "dataset", "bench",
]
